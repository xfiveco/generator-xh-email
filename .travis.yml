sudo: false
language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-email-boilerplate' ]; then cd .. && eval "mv $currentfolder generator-email-boilerplate" && cd generator-email-boilerplate; fi
